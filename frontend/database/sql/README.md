# Database SQL

Custom SQL belongs here only when it cannot reasonably be represented by an Alembic migration.

Production schema changes must be implemented through backend Alembic migrations.

Never execute destructive SQL automatically against production.
